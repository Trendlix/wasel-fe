import ContactClient from "@/shared/components/pages/contact/contact-client";

const Contact = () => {
    return (
        <main>
            <ContactClient />
        </main>
    );
};

export default Contact;